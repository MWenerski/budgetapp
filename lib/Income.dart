import 'package:flutter/material.dart';

class Income extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Income.dart'),
      ),
      body: Center(
        child: Text('This is Income.dart - Page 3.'),
      ),
    );
  }
}